platform: multi-submission
os: ubuntu-22

hooks:
  build:
    steps:
      - name: Create submission directories
        run:
          - mkdir -p /pedagog/results
          - mkdir -p /pedagog/workspace
  entrypoint: /pedagog/run.sh
